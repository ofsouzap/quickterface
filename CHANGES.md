# 0.1.1

Added `js_of_ocaml-compiler` dependency to `quickterface_web_app` web app library

# 0.1.0

Initial, work-in-progress version with:

- Web app and TUI targets
- Text, integer, single-selection and multi-selection inputs
- Text, title and math outputs
